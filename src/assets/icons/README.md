# Icons Folder

This folder contains icon image assets.

## Note
This project uses `lucide-react-native` for icons, so you may not need many icon images here.
However, you can store custom icons or brand-specific icons in this folder.

## Usage
```typescript
import customIcon from '../assets/icons/custom-icon.png';

<Image source={customIcon} style={styles.icon} />
```
